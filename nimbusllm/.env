# App
ENV=dev

# DB (Docker network host is service name: db, use localhost for Windows local dev)
DATABASE_URL=postgresql+psycopg2://nimbus:nimbus@localhost:5433/nimbusllm

# For later (when you add providers)
OPENAI_API_KEY=
GEMINI_API_KEY=
